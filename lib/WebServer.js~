const WifiScanner = require('./WifiScanner');

const express = require('express')
const app = express()
const wifiscn = new WifiScanner(); 
app.get('/', function (req, res) {
	let wifiNetworks = wifiscn.Scan();
  res.status(200).json(wifiNetworks);
})
 
app.listen(80);